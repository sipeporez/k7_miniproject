package com.subway;

import org.springframework.boot.ApplicationArguments;
import org.springframework.boot.ApplicationRunner;
import org.springframework.security.core.userdetails.UsernameNotFoundException;
import org.springframework.security.crypto.password.PasswordEncoder;
import org.springframework.stereotype.Component;

import com.subway.domain.Board;
import com.subway.domain.Member;
import com.subway.domain.Role;
import com.subway.persistence.BoardRepository;
import com.subway.persistence.MemberRepository;

import lombok.RequiredArgsConstructor;

@Component
@RequiredArgsConstructor
public class BoardInit implements ApplicationRunner{
	private final MemberRepository mr;
	private final BoardRepository br;
	@Override
	public void run(ApplicationArguments args) throws Exception {
		
		Member member = mr.findById(member.getUserid()).orElseThrow(()->new throw UsernameNotFoundException))
		
		br.save(Board.builder()
				.title("테스트용 게시글")
				.content("테스트용 내용")
				.member(getUserId())
				.build());
		br.save(Member.builder()
				.userid("bbbb")
				.password(enc.encode("11"))
				.nickname("BB")
				.role(Role.ROLE_ADMIN)
				.build());
		
	}

}
